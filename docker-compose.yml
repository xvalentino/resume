version: "3"

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "1234:1234"
    volumes:
      - .:/root
  production:
    build: .
    ports:
      - "80:1234"
